server:
  port: 9999
spring:
  application:
    name: eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9999/eureka
    register-with-eureka: false
    fetch-registry: false
